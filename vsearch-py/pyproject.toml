[project]
name = "vsearch-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
script = "main.py"
dependencies = [
    "fastembed>=0.8.0",
]
